{
  "hooks": {
    "PreCommit": [
      { "command": "uv run ruff check --fix ." },
      { "command": "uv run ruff format --check ." },
      { "command": "uv run python -m mypy app/ --ignore-missing-imports" }
    ]
  }
}
