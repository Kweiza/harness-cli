# Code Quality

- Type safety first — minimize `any`/`unknown` usage
- No hardcoded values — use environment variables or config files
- Single Responsibility Principle — one purpose per function/class
- No magic numbers — define as named constants
- Prefer explicit over implicit — name things clearly
- No unused imports or variables
- Keep functions under 50 lines — extract when longer
