# Documentation

- README.md required: project description, install steps, run commands
- API endpoints documented via OpenAPI spec or inline comments
- Only add inline comments for complex business logic — code should be self-documenting
- Keep documentation close to the code it describes
